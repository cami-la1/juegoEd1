//---------------------------------------------------------------------------

#ifndef mapaH
#define mapaH
//---------------------------------------------------------------------------
//---------------------------------------------------------------------------
#include "Matriz_1N.h" #include "matrizCCcpp.h"// La clase Mapa necesita saber qué es una MatrizDispersa
class Mapa {
public:
    // Constructor (no necesita hacer nada)
    Mapa();
    // Método principal para configurar el tablero
	void cargarMapaInicial(MatrizCC* matriz, int filas, int columnas);
};
//---------------------------------------------------------------------------

#endif
